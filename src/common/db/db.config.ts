const devServer: String = 'http://localhost:8888/v1/'

export const config = {
    collections: {
        schools: {
            server: devServer
        },
        users: {
            server: devServer
        }
    }
}